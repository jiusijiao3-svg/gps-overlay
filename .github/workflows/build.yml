private fun writeAndVerifyGeotag(file: File, dateTaken: Long) {
    val snapshot = gpsLog.toList()
    if (snapshot.isEmpty()) {
        showHudBanner("⚠ 失敗: 生GPSログが空です", false)
        return
    }

    val nearWindow = snapshot.filter { abs(it.timeMs - dateTaken) < 10_000 }
    val usedStationary = nearWindow.isNotEmpty() && isStationary(nearWindow)
    val record = if (usedStationary) {
        weightedAverage(nearWindow)
    } else {
        snapshot.minByOrNull { abs(it.timeMs - dateTaken) }
    }

    if (record == null || abs(record.timeMs - dateTaken) > 30_000) {
        showHudBanner("⚠ 失敗: 撮影時刻の生GPSログなし", false)
        return
    }

    // ★書き込み"前"の値を必ず記録する
    val beforeExif = try { ExifInterface(file.absolutePath) } catch (e: Exception) { null }
    val beforeLatLon = beforeExif?.latLong

    Log.i(TAG, "=== 同期試行 ===")
    Log.i(TAG, "対象ファイル: ${file.absolutePath}")
    Log.i(TAG, "撮影時刻: $dateTaken / 使用ログ数: ${nearWindow.size} / 静止判定: $usedStationary")
    Log.i(TAG, "書き込み前Exif座標: $beforeLatLon")
    Log.i(TAG, "書き込む座標: lat=${record.lat}, lon=${record.lon}, acc=${record.acc}")
    Log.i(TAG, "衛星: $satellitesUsed/$satellitesTotal")

    try {
        val exif = ExifInterface(file.absolutePath)
        exif.setLatLong(record.lat, record.lon)
        exif.setAltitude(record.alt)
        exif.setAttribute(ExifInterface.TAG_GPS_PROCESSING_METHOD, "RAW_GNSS_OVERLAY")
        exif.saveAttributes()

        val verifyExif = ExifInterface(file.absolutePath)
        val afterLatLon = verifyExif.latLong

        Log.i(TAG, "書き込み後Exif座標: ${afterLatLon?.get(0)}, ${afterLatLon?.get(1)}")

        // 前後で座標が実際に変化したかどうかを明示的に判定
        val actuallyChanged = beforeLatLon == null ||
            abs(beforeLatLon[0] - (afterLatLon?.get(0) ?: 0.0)) > 0.00001 ||
            abs(beforeLatLon[1] - (afterLatLon?.get(1) ?: 0.0)) > 0.00001

        val isVerified = afterLatLon != null &&
            abs(afterLatLon[0] - record.lat) < 0.0001 &&
            abs(afterLatLon[1] - record.lon) < 0.0001

        Log.i(TAG, "値の変化: $actuallyChanged / 意図した値と一致: $isVerified")

        if (isVerified) {
            processedPaths.add(file.absolutePath)
            MediaScannerConnection.scanFile(applicationContext, arrayOf(file.absolutePath), arrayOf("image/jpeg")) { _, _ -> }
            val tag = if (actuallyChanged) "変化あり" else "変化なし(元々同じ値)"
            showHudBanner("✓ 完了[$tag]: ±${record.acc.toInt()}m ${if (usedStationary) "(${nearWindow.size}点平均)" else "(1点)"}", true)
        } else {
            showHudBanner("⚠ 失敗: 書き込み後の照合に不一致", false)
        }
    } catch (e: Exception) {
        Log.e(TAG, "Exif書き込み例外", e)
        showHudBanner("⚠ 失敗: ${e.javaClass.simpleName}", false)
    }
}
